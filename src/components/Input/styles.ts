import { StyleSheet } from "react-native"

export const styles = StyleSheet.create({
    
  container: {
    backgroundColor: '#fff',
    height: 48,
    width: '100%',
    paddingHorizontal : 16,
    borderWidth: 1, 
    borderRadius: 8,
    borderColor: '#C5C3CB',
  },
  title: {
    color: '#fff',
    fontSize: 16,
    fontWeight: 'bold',
  },
});
